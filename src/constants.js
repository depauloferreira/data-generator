const MIN_HWM = Math.pow(10, 1);
const LOW_HWM = Math.pow(10, 3);
const MID_HWM = Math.pow(10, 6);
const HIG_HWM = Math.pow(10, 8);

export const HWM = {
    MIN: MIN_HWM,
    LOW: LOW_HWM,
    MID: MID_HWM,
    HIG: HIG_HWM
}